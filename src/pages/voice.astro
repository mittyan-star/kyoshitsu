---
import BaseLayout from '../layouts/BaseLayout.astro';
import { breadcrumbSchema } from '../lib/schema';

const voices = [
  {
    name: '中2 保護者',
    comment:
      'ワンフレーズ報告で、毎回の授業内容がすぐに伝わり家庭での会話が増えました。自分の言葉で説明する力が伸びているのを感じます。',
  },
  {
    name: '高1 生徒',
    comment:
      '京大数学の過去問を先生に説明する時間が刺激的。途中で詰まったときも、質問で考えを深められるので前向きに挑戦できます。',
  },
  {
    name: 'Cosmos Party参加者',
    comment: '地域の方と一緒に学びを発表でき、子どもたちの目が輝いていました。交流タイムで次の企画が生まれました。',
  },
];

const structuredData = [
  breadcrumbSchema([
    { name: 'ホーム', item: 'https://mittyan-star.github.io/kyoshitsu/' },
    { name: '保護者・生徒の声', item: 'https://mittyan-star.github.io/kyoshitsu/voice' },
  ]),
  {
    '@context': 'https://schema.org',
    '@type': 'ItemList',
    name: '保護者・生徒の声',
    itemListElement: voices.map((voice, index) => ({
      '@type': 'ListItem',
      position: index + 1,
      name: voice.name,
      item: {
        '@type': 'Review',
        reviewBody: voice.comment,
        author: {
          '@type': 'Person',
          name: voice.name,
        },
      },
    })),
  },
];
---
<BaseLayout
  title="保護者・生徒の声｜ワンフレーズ報告で見える成長"
  description="みっちゃん塾に通う保護者・生徒・地域の方からの声を紹介。ワンフレーズ報告やプレゼン教育の効果をご覧ください。"
  path="/voice"
  summary="ワンフレーズ報告で家庭との会話が増え、プレゼン教育で自信を得たという声が届いています。"
  breadcrumbs={[{ href: '/voice', label: '保護者・生徒の声' }]}
  structuredData={structuredData}
>
  <section class="section">
    <h1 class="text-3xl font-bold text-slate-900">声を集めました</h1>
    <div class="space-y-4">
      {voices.map((voice) => (
        <blockquote class="rounded-xl border border-slate-200 bg-white p-6 shadow-sm">
          <p class="text-sm leading-relaxed text-slate-700">“{voice.comment}”</p>
          <cite class="mt-3 block text-xs font-semibold text-slate-500">{voice.name}</cite>
        </blockquote>
      ))}
    </div>
  </section>
</BaseLayout>
